# large run outputs — flagship checkpoint ships via GitHub Release, not git
# (logs/ IS tracked: eval transcripts and training logs are the paper's evidence base)
checkpoints/
data/
wandb/

# legacy phases: code, docs, and logs are tracked; heavy data is not
legacy/phase1/datasets/
legacy/phase0/artifacts/

# env / build
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
.claude/

# paper build artifacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.toc
paper/*.fls
paper/*.fdb_latexmk

# staged upload kit — never tracked; delete after publishing
publish/
